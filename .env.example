# =============================================================================
# AllyCare Demo - Environment Configuration Template
# =============================================================================
# 
# This file contains the environment variables needed to run AllyCare.
# Copy this file to .env and fill in your actual values.
# 
# IMPORTANT: Never commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Firebase Configuration
# -----------------------------------------------------------------------------
# Get these values from your Firebase project settings
# Go to: Firebase Console > Project Settings > General > Your apps

# Firebase Project Configuration
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=your-sender-id

# Firebase API Keys
FIREBASE_API_KEY_ANDROID=your-android-api-key
FIREBASE_API_KEY_IOS=your-ios-api-key
FIREBASE_API_KEY_WEB=your-web-api-key

# Firebase App IDs
FIREBASE_APP_ID_ANDROID=your-android-app-id
FIREBASE_APP_ID_IOS=your-ios-app-id
FIREBASE_APP_ID_WEB=your-web-app-id

# Firebase Analytics (Optional)
FIREBASE_MEASUREMENT_ID=your-measurement-id

# -----------------------------------------------------------------------------
# Platform Specific Configuration
# -----------------------------------------------------------------------------
# iOS Bundle ID (used in firebase_config.dart)
IOS_BUNDLE_ID=com.example.ally_care_demo

# -----------------------------------------------------------------------------
# Development Configuration (Optional)
# -----------------------------------------------------------------------------
# Application Environment
APP_ENV=development

# Firebase Emulator Configuration (Optional - for development)
USE_FIREBASE_EMULATOR=false
FIREBASE_EMULATOR_HOST=localhost
FIREBASE_AUTH_EMULATOR_PORT=9099
FIRESTORE_EMULATOR_PORT=8080
FIREBASE_STORAGE_EMULATOR_PORT=9199

# =============================================================================
# Setup Instructions:
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual Firebase configuration values
# 3. Update the IOS_BUNDLE_ID if you changed the bundle identifier
# 4. Set USE_FIREBASE_EMULATOR=true if you want to use Firebase emulators
# 5. Never commit the .env file to version control
# =============================================================================